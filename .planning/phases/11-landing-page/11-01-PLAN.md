---
phase: 11-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/CNAME
  - site/index.html
  - site/css/styles.css
  - site/assets/director-logo.png
  - site/assets/favicon.png
autonomous: true

must_haves:
  truths:
    - "Opening site/index.html in a browser shows a dark-mode page with the Director logo in the nav"
    - "Hero section displays a bold tagline, supporting subtext, and two CTAs (install + GitHub)"
    - "Clicking Copy on the install command copies the text to clipboard"
    - "Sticky navigation appears with anchor links to all page sections"
    - "All 9 section shells exist in the HTML with correct IDs for anchor navigation"
  artifacts:
    - path: "site/index.html"
      provides: "Complete HTML page with head, nav, hero, and 9 section shells"
      contains: "director.cc"
    - path: "site/css/styles.css"
      provides: "Full design system and component styles"
      contains: "--color-accent: #3B82F6"
    - path: "site/CNAME"
      provides: "GitHub Pages custom domain configuration"
      contains: "director.cc"
    - path: "site/assets/director-logo.png"
      provides: "White wordmark logo for dark background"
    - path: "site/assets/favicon.png"
      provides: "Square /d mark for browser tab"
  key_links:
    - from: "site/index.html"
      to: "site/css/styles.css"
      via: "stylesheet link"
      pattern: "link.*styles\\.css"
    - from: "site/index.html"
      to: "site/assets/director-logo.png"
      via: "img src in nav"
      pattern: "img.*director-logo"
    - from: "site/index.html"
      to: "site/assets/favicon.png"
      via: "link rel=icon"
      pattern: "link.*icon.*favicon"
---

<objective>
Create the site foundation (directory structure, assets, design system) and the complete hero section with sticky navigation and dual CTAs.

Purpose: Establishes the visual identity, design system, and primary conversion point (hero) that all subsequent plans build upon.
Output: A functional dark-mode landing page with hero section, navigation, and section shells ready for content.
</objective>

<execution_context>
@/Users/noahrasheta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/noahrasheta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-landing-page/11-CONTEXT.md
@.planning/phases/11-landing-page/11-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create site directory structure, assets, CNAME, and full HTML skeleton with CSS design system</name>
  <files>site/CNAME, site/index.html, site/css/styles.css, site/assets/director-logo.png, site/assets/favicon.png</files>
  <action>
1. Create the `site/` directory structure: `site/css/`, `site/js/`, `site/assets/`

2. Copy logo assets:
   - Copy `docs/logos/director-logo-light.png` to `site/assets/director-logo.png` (white text wordmark -- for dark background)
   - Copy `docs/logos/director-logo-sq-dark.png` to `site/assets/favicon.png` (white /d on dark square -- for browser tab)

3. Create `site/CNAME` containing exactly: `director.cc` (no protocol, no trailing slash, no newline)

4. Create `site/index.html` with:
   - Full `<head>`: charset UTF-8, viewport meta, title "Director — Orchestration for Vibe Coders", description meta, Open Graph tags (og:title, og:description, og:image pointing to https://director.cc/assets/og-image.png, og:url, og:type), Twitter Card tags (summary_large_image), favicon link to assets/favicon.png, Google Fonts preconnect + stylesheet link for Inter (weights 400,500,600,700,800) and JetBrains Mono (400,500), stylesheet link to css/styles.css
   - `<body>` with semantic structure containing these sections as shells (with IDs for anchor nav):
     - `<nav>` with class "nav" -- sticky navigation
     - `<section id="hero">` -- Hero
     - `<section id="problem">` -- The Problem
     - `<section id="how-it-works">` -- How Director Works (terminal walkthrough)
     - `<section id="features">` -- What Director Does
     - `<section id="why">` -- Why Director
     - `<section id="get-started">` -- Get Started (mid-page CTA)
     - `<section id="commands">` -- Commands
     - `<section id="newsletter">` -- Newsletter
     - `<footer id="footer">` -- Footer
   - Script tag at end of body linking to `js/main.js`
   - Each section shell should have a `class="section"` and a `<div class="container">` inside for max-width constraint
   - Sections below hero should additionally have a placeholder comment like `<!-- Content in plan 02/03 -->`

5. Create `site/css/styles.css` with the complete design system:
   - CSS reset (box-sizing, margin/padding reset, img max-width)
   - `:root` custom properties per research:
     - Background layers: `--color-bg: #0A0A0A`, `--color-bg-elevated: #141414`, `--color-bg-surface: #1C1C1C`, `--color-bg-overlay: rgba(255,255,255,0.04)`
     - Text: `--color-text-primary: #EDEDED`, `--color-text-secondary: #A1A1A1`, `--color-text-muted: #666666`
     - Accent: `--color-accent: #3B82F6`, `--color-accent-hover: #2563EB`, `--color-accent-subtle: rgba(59,130,246,0.12)`
     - Borders: `--color-border: rgba(255,255,255,0.08)`, `--color-border-accent: rgba(59,130,246,0.3)`
     - Fonts: `--font-body: 'Inter', system-ui, -apple-system, sans-serif`, `--font-mono: 'JetBrains Mono', 'Fira Code', monospace`
     - Spacing: `--section-padding: clamp(4rem, 8vw, 8rem)`, `--container-max: 1200px`, `--container-narrow: 800px`
   - `html`: scroll-behavior smooth, scroll-padding-top 5rem, background var(--color-bg), color var(--color-text-secondary), font-family var(--font-body)
   - `@media (prefers-reduced-motion: reduce)` disabling scroll-behavior
   - `.container`: max-width var(--container-max), margin 0 auto, padding 0 1.5rem
   - `.container--narrow`: max-width var(--container-narrow)
   - `.section`: padding var(--section-padding) 0
   - Responsive typography classes using clamp():
     - `.hero-title`: clamp(2.5rem, 5vw + 1rem, 5rem), weight 800, line-height 1.1, letter-spacing -0.02em, color var(--color-text-primary)
     - `.hero-subtitle`: clamp(1.125rem, 1.5vw + 0.5rem, 1.5rem), weight 400, line-height 1.6, color var(--color-text-secondary)
     - `.section-title`: clamp(1.75rem, 3vw + 0.5rem, 3rem), weight 700, line-height 1.2, color var(--color-text-primary)
     - `.section-subtitle`: clamp(1rem, 1.2vw + 0.5rem, 1.25rem), line-height 1.6, color var(--color-text-secondary)
   - Navigation styles:
     - `.nav`: position sticky, top 0, z-index 100, backdrop-filter blur(12px), background rgba(10,10,10,0.85), border-bottom 1px solid var(--color-border), padding 0.75rem 0
     - `.nav .container`: display flex, align-items center, justify-content space-between
     - `.nav-logo img`: height 1.5rem (the wordmark)
     - `.nav-links`: display flex, gap 1.5rem, list-style none
     - `.nav-links a`: color var(--color-text-muted), text-decoration none, font-size 0.875rem, font-weight 500, transition color 0.2s
     - `.nav-links a:hover`, `.nav-links a.active`: color var(--color-text-primary)
     - Mobile: hide nav-links on small screens (display none below 768px), show hamburger icon (implementation optional, can be basic)
   - Button styles:
     - `.btn`: display inline-flex, align-items center, gap 0.5rem, padding 0.75rem 1.5rem, border-radius 0.5rem, font-weight 600, font-size 1rem, text-decoration none, transition all 0.2s, cursor pointer, border none
     - `.btn--primary`: background var(--color-accent), color white, hover background var(--color-accent-hover)
     - `.btn--secondary`: background transparent, color var(--color-text-primary), border 1px solid var(--color-border), hover border-color var(--color-text-muted)
   - Install block styles:
     - `.install-block`: display flex, align-items center, background var(--color-bg-elevated), border 1px solid var(--color-border), border-radius 0.5rem, padding 0.75rem 1rem, font-family var(--font-mono), font-size 0.875rem, max-width fit-content, gap 1rem
     - `.install-command`: color var(--color-text-primary), white-space nowrap, overflow-x auto
     - `.copy-btn`: background none, border 1px solid var(--color-border), color var(--color-accent), padding 0.375rem 0.75rem, border-radius 0.25rem, cursor pointer, font-size 0.8125rem, font-weight 500, transition all 0.2s, white-space nowrap
     - `.copy-btn:hover`: border-color var(--color-accent), background var(--color-accent-subtle)
  </action>
  <verify>
Open `site/index.html` in a browser (or `python3 -m http.server 8000 --directory site/`). The page should show:
- Dark background (#0A0A0A)
- Navigation bar with Director logo visible (white on dark)
- Favicon visible in browser tab (/d mark)
- 9 empty section shells exist in the DOM (inspect elements)
- Google Fonts (Inter, JetBrains Mono) loading
  </verify>
  <done>site/ directory exists with CNAME, index.html skeleton, complete CSS design system, and copied logo assets. Opening in browser shows dark-mode page with navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Build complete hero section with tagline, CTAs, and copy-to-clipboard</name>
  <files>site/index.html, site/css/styles.css</files>
  <action>
1. Populate the `<nav>` element in `index.html`:
   - Logo: `<a href="#hero" class="nav-logo"><img src="assets/director-logo.png" alt="Director" /></a>`
   - Nav links list: Problem (#problem), How It Works (#how-it-works), Features (#features), Why (#why), Commands (#commands), Get Started (#get-started)
   - Mobile menu button (hamburger) that toggles nav-links visibility -- a simple `<button class="nav-toggle">` with three-line icon via CSS (or SVG)

2. Populate the `<section id="hero">` with:
   - Structure: `<div class="container hero-content">`
   - Hero title (h1, class hero-title): "You have the ideas.<br>Now you have the structure." (per CONTEXT.md: lead with aspiration, confidence hook)
   - Hero subtitle (p, class hero-subtitle): "Director is an orchestration plugin for Claude Code that turns solo builders into vibe coders. From vision to shipped product — guided every step." (plain language, introduces the vibe coder identity)
   - CTA group (`<div class="hero-ctas">`):
     - Install block with copy button:
       ```html
       <div class="install-block">
         <code class="install-command" id="installCmd">/plugin marketplace add noahrasheta/director && /plugin install director@director-marketplace</code>
         <button class="copy-btn" onclick="copyInstall()" aria-label="Copy install command">
           <span class="copy-icon">Copy</span>
           <span class="copy-success" hidden>Copied!</span>
         </button>
       </div>
       ```
     - GitHub link: `<a href="https://github.com/noahrasheta/director" class="btn btn--secondary" target="_blank" rel="noopener">View on GitHub</a>`
   - Prerequisites note: `<p class="hero-prereq">Requires <a href="https://claude.ai/code">Claude Code</a> v1.0.33+</p>` (brief, one line, per research recommendation)

3. Add inline `<script>` at end of body (before the js/main.js link) for copy-to-clipboard:
   ```javascript
   async function copyInstall() {
     const text = document.getElementById('installCmd').textContent.trim();
     try {
       await navigator.clipboard.writeText(text);
       const btn = document.querySelector('.copy-btn');
       btn.querySelector('.copy-icon').hidden = true;
       btn.querySelector('.copy-success').hidden = false;
       setTimeout(() => {
         btn.querySelector('.copy-icon').hidden = false;
         btn.querySelector('.copy-success').hidden = true;
       }, 2000);
     } catch (err) {
       const range = document.createRange();
       range.selectNodeContents(document.getElementById('installCmd'));
       window.getSelection().removeAllRanges();
       window.getSelection().addRange(range);
     }
   }
   ```

4. Add hero-specific styles to `styles.css`:
   - `.hero-content`: text-align center, padding-top clamp(4rem, 10vw, 8rem), padding-bottom var(--section-padding)
   - `.hero-title`: margin-bottom 1.5rem (plus existing responsive font styles)
   - `.hero-subtitle`: max-width 40rem, margin 0 auto 2.5rem auto
   - `.hero-ctas`: display flex, flex-direction column, align-items center, gap 1rem
   - `.hero-prereq`: font-size 0.8125rem, color var(--color-text-muted), margin-top 1.5rem
   - `.hero-prereq a`: color var(--color-accent), text-decoration none
   - `.hero-prereq a:hover`: text-decoration underline
   - Mobile nav toggle styles:
     - `.nav-toggle`: display none (visible only below 768px), background none, border none, color var(--color-text-primary), cursor pointer, padding 0.5rem
     - Below 768px: `.nav-toggle` display block, `.nav-links` display none, `.nav-links.open` display flex + flex-direction column + position absolute + top 100% + left 0 + right 0 + background var(--color-bg) + padding 1rem + border-bottom 1px solid var(--color-border)
   - On mobile (below 768px), the install-block should allow horizontal scroll: `.install-block` overflow-x auto, max-width 100%

5. Add mobile nav toggle JS in the inline script:
   ```javascript
   document.querySelector('.nav-toggle')?.addEventListener('click', () => {
     document.querySelector('.nav-links')?.classList.toggle('open');
   });
   ```
  </action>
  <verify>
Open `site/index.html` in a browser (`python3 -m http.server 8000 --directory site/`):
1. Hero shows bold tagline, supporting text, install command block with Copy button, and GitHub link
2. Clicking Copy shows "Copied!" for 2 seconds and places the install command on clipboard
3. Navigation has Director logo and section links
4. Clicking a nav link scrolls to the correct section
5. Page looks good at both desktop (1200px+) and mobile (375px) widths
6. Favicon shows /d mark in browser tab
  </verify>
  <done>Hero section is complete with bold tagline, dual CTAs (copy-to-clipboard install + GitHub), prerequisites note, sticky navigation with all section links, and mobile-responsive layout.</done>
</task>

</tasks>

<verification>
- `site/` directory exists with `CNAME`, `index.html`, `css/styles.css`, `assets/director-logo.png`, `assets/favicon.png`
- Opening `site/index.html` in a browser shows dark-mode page
- Hero section has bold tagline, install command with copy button, GitHub link
- Navigation sticks to top on scroll
- Copy button works (test on localhost with python3 server)
- All 9 section IDs exist in the HTML
- Page is responsive (mobile and desktop)
</verification>

<success_criteria>
- Dark-mode landing page loads with Director branding
- Hero tagline visible, dual CTAs functional
- Copy-to-clipboard works for install command
- Sticky nav with anchor links to all sections
- Design system established (CSS custom properties)
- Mobile responsive
</success_criteria>

<output>
After completion, create `.planning/phases/11-landing-page/11-01-SUMMARY.md`
</output>
