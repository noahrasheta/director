---
phase: 11-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - site/index.html
  - site/css/styles.css
  - site/js/main.js
autonomous: true

must_haves:
  truths:
    - "Scrolling past the hero reveals a Problem section that describes what vibe coders struggle with"
    - "Terminal animation plays automatically when scrolled into view, showing onboard -> blueprint -> build"
    - "Terminal has a replay button to watch the animation again"
    - "Features section shows Director's three-part loop (Blueprint, Build, Inspect) with descriptions"
    - "Why Director section differentiates from competitors without naming them"
    - "Commands section lists all 12 Director commands with descriptions"
  artifacts:
    - path: "site/js/main.js"
      provides: "Terminal animation class with IntersectionObserver trigger and replay"
      contains: "TerminalAnimation"
    - path: "site/index.html"
      provides: "Content for Problem, How It Works, Features, Why, Commands sections"
      contains: "onboard"
    - path: "site/css/styles.css"
      provides: "Styles for all content sections, terminal, feature cards, command table"
      contains: ".terminal"
  key_links:
    - from: "site/index.html"
      to: "site/js/main.js"
      via: "script tag"
      pattern: "script.*main\\.js"
    - from: "site/js/main.js"
      to: "IntersectionObserver"
      via: "viewport detection for terminal animation"
      pattern: "IntersectionObserver"
---

<objective>
Build all content sections: Problem, Terminal walkthrough, Features, Why Director, and Commands.

Purpose: These sections tell Director's full story -- from identifying with the visitor's pain, through a visual demo, to feature breakdown and differentiation. This is the persuasion layer of the page.
Output: Five complete content sections with an interactive terminal animation.
</objective>

<execution_context>
@/Users/noahrasheta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/noahrasheta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-landing-page/11-CONTEXT.md
@.planning/phases/11-landing-page/11-RESEARCH.md
@.planning/phases/11-landing-page/11-01-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Problem section, terminal walkthrough with animation, and How It Works section</name>
  <files>site/index.html, site/css/styles.css, site/js/main.js</files>
  <action>
**Problem section (`#problem`):**

Replace the placeholder in the problem section with compelling content per CONTEXT.md messaging decisions:
- Section title (h2, class section-title): "AI can write code. But who's keeping track?"
- 2-3 short paragraphs (class section-subtitle) that build empathy. The narrative arc:
  1. You start with an idea and AI helps you build fast
  2. But soon the project grows -- context gets lost, files pile up, you forget what was built and why
  3. You need structure, not more code. You need someone keeping track of the big picture while AI handles the details.
- Keep it scannable -- no walls of text. Short paragraphs, punchy sentences.
- Tone: empathetic, not condescending. "You've felt this" not "You're doing it wrong."

**How It Works section (`#how-it-works`):**

This section has TWO parts: the terminal animation and a brief explanation.

Part A -- Terminal animation:
- Create a terminal window component in the HTML:
  ```html
  <div class="terminal-window">
    <div class="terminal-header">
      <span class="terminal-dot"></span>
      <span class="terminal-dot"></span>
      <span class="terminal-dot"></span>
      <span class="terminal-title">Claude Code</span>
    </div>
    <div class="terminal-body" id="terminal"></div>
    <button class="terminal-replay" id="replayBtn" hidden>Replay</button>
  </div>
  ```
- The terminal animation script demonstrates the Director workflow in 3 acts:
  - Act 1 (Onboard): User types `/director:onboard` → Output: "What do you want to build?" → User answers "A task management app" → Output: "Got it. I've captured your vision..."
  - Act 2 (Blueprint): User types `/director:blueprint` → Output: "Breaking that down into steps..." → Output shows goal/step/task hierarchy briefly (2-3 lines)
  - Act 3 (Build): User types `/director:build` → Output: "Working on: Set up the database..." → Output: "Progress saved. Next up: Create the API."
- Keep the animation concise -- ~12-15 lines total. Each act should feel like a natural conversation.

Part B -- Explanation:
- Below the terminal, add a brief text block explaining the three-part loop: Blueprint (capture vision, create gameplan), Build (execute tasks with fresh AI context), Inspect (verify outcomes, not just completion)
- Use a simple 3-column layout on desktop (1 column mobile) with the loop name, an icon (emoji or simple SVG -- keep it minimal), and one sentence each

**CSS styles to add:**

Terminal window:
- `.terminal-window`: background var(--color-bg-elevated), border 1px solid var(--color-border), border-radius 0.75rem, overflow hidden, max-width 700px, margin 0 auto 3rem auto, box-shadow 0 4px 24px rgba(0,0,0,0.4)
- `.terminal-header`: display flex, align-items center, gap 0.5rem, padding 0.75rem 1rem, background rgba(255,255,255,0.03), border-bottom 1px solid var(--color-border)
- `.terminal-dot`: width 0.75rem, height 0.75rem, border-radius 50%, background var(--color-text-muted), opacity 0.5
- `.terminal-dot:first-child`: background #FF5F57
- `.terminal-dot:nth-child(2)`: background #FEBC2E
- `.terminal-dot:nth-child(3)`: background #28C840
- `.terminal-title`: margin-left auto, margin-right auto, font-size 0.75rem, color var(--color-text-muted)
- `.terminal-body`: padding 1.25rem, font-family var(--font-mono), font-size 0.875rem, line-height 1.7, min-height 300px
- `.terminal-line`: margin-bottom 0.25rem
- `.terminal-line.input`: color var(--color-text-primary)
- `.terminal-prompt`: color var(--color-accent), margin-right 0.5rem
- `.terminal-line.output`: color var(--color-text-secondary)
- `.terminal-line.fade-in`: animation fadeIn 0.3s ease
- `@keyframes fadeIn`: from opacity 0, transform translateY(0.25rem); to opacity 1, transform translateY(0)
- `.terminal-replay`: display inline-flex, align-items center, gap 0.375rem, margin 0.75rem 1.25rem, padding 0.375rem 0.75rem, background none, border 1px solid var(--color-border), color var(--color-text-muted), border-radius 0.25rem, cursor pointer, font-size 0.75rem, font-family var(--font-body), transition all 0.2s
- `.terminal-replay:hover`: color var(--color-text-primary), border-color var(--color-text-muted)

Three-part loop layout:
- `.loop-grid`: display grid, grid-template-columns repeat(3, 1fr), gap 2rem, margin-top 2rem
- Below 768px: `.loop-grid` grid-template-columns 1fr
- `.loop-item`: text-align center (or left on mobile)
- `.loop-icon`: font-size 2rem, margin-bottom 0.75rem (use text emoji: Blueprint, Build, Inspect)
- `.loop-name`: font-weight 700, color var(--color-text-primary), margin-bottom 0.5rem
- `.loop-desc`: color var(--color-text-secondary), font-size 0.9375rem, line-height 1.5

**JavaScript (`site/js/main.js`):**

Create the TerminalAnimation class per the research pattern:
```javascript
class TerminalAnimation {
  constructor(element, lines, options = {}) {
    this.el = element;
    this.lines = lines;
    this.typeDelay = options.typeDelay || 40;
    this.lineDelay = options.lineDelay || 600;
    this.outputDelay = options.outputDelay || 300;
    this.running = false;
  }

  async type(text, container) {
    for (const char of text) {
      container.textContent += char;
      await this.delay(this.typeDelay);
    }
  }

  async run() {
    if (this.running) return;
    this.running = true;
    this.el.innerHTML = '';

    const replayBtn = document.getElementById('replayBtn');
    if (replayBtn) replayBtn.hidden = true;

    for (const line of this.lines) {
      const lineEl = document.createElement('div');
      lineEl.className = `terminal-line ${line.type}`;

      if (line.type === 'input') {
        const prompt = document.createElement('span');
        prompt.className = 'terminal-prompt';
        prompt.textContent = '$ ';
        lineEl.appendChild(prompt);
        const text = document.createElement('span');
        lineEl.appendChild(text);
        this.el.appendChild(lineEl);
        await this.type(line.text, text);
        await this.delay(this.lineDelay);
      } else if (line.type === 'blank') {
        lineEl.innerHTML = '&nbsp;';
        this.el.appendChild(lineEl);
        await this.delay(this.outputDelay);
      } else {
        lineEl.textContent = line.text;
        this.el.appendChild(lineEl);
        lineEl.classList.add('fade-in');
        await this.delay(this.outputDelay);
      }
    }

    this.running = false;
    if (replayBtn) replayBtn.hidden = false;
  }

  delay(ms) {
    return new Promise(resolve => setTimeout(resolve, ms));
  }
}
```

Define the terminal content lines array (the 3-act walkthrough described above). Example:
```javascript
const terminalLines = [
  { type: 'input', text: '/director:onboard' },
  { type: 'output', text: 'What do you want to build?' },
  { type: 'input', text: 'A task management app with team collaboration' },
  { type: 'output', text: 'Got it. Vision captured. Run /director:blueprint when ready.' },
  { type: 'blank' },
  { type: 'input', text: '/director:blueprint' },
  { type: 'output', text: 'Breaking your vision into steps...' },
  { type: 'output', text: '  Goal 1: Core App — 4 steps, 12 tasks' },
  { type: 'output', text: '  Step 1: Database and auth (3 tasks, ready to build)' },
  { type: 'output', text: 'Gameplan created. Ready to build.' },
  { type: 'blank' },
  { type: 'input', text: '/director:build' },
  { type: 'output', text: 'Working on: Set up the database...' },
  { type: 'output', text: 'Progress saved. Next up: Create user authentication.' },
];
```

Set up IntersectionObserver to trigger animation when terminal scrolls into view:
```javascript
const terminal = document.getElementById('terminal');
const anim = new TerminalAnimation(terminal, terminalLines);

const terminalObserver = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      anim.run();
      terminalObserver.unobserve(entry.target);
    }
  });
}, { threshold: 0.3 });

terminalObserver.observe(terminal);

document.getElementById('replayBtn')?.addEventListener('click', () => anim.run());
```

Respect `prefers-reduced-motion` -- if enabled, display all terminal lines instantly (skip animation):
```javascript
if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
  // Show all lines immediately without animation
  terminalLines.forEach(line => { /* append all at once */ });
}
```
  </action>
  <verify>
Open `site/index.html` via `python3 -m http.server 8000 --directory site/`:
1. Problem section shows empathetic content about vibe coder struggles
2. Scrolling to the terminal triggers the typing animation
3. Animation shows onboard -> blueprint -> build workflow
4. Replay button appears after animation completes; clicking it replays
5. Three-part loop explanation appears below the terminal (Blueprint, Build, Inspect)
6. Terminal looks like a real terminal window with colored dots and title
  </verify>
  <done>Problem section has compelling copy. Terminal walkthrough animates on scroll, showing the full Director workflow. Replay button works. Three-part loop explanation is clear and scannable.</done>
</task>

<task type="auto">
  <name>Task 2: Features, Why Director, and Commands sections</name>
  <files>site/index.html, site/css/styles.css</files>
  <action>
**Features section (`#features`) -- "What Director Does":**

Structure as a feature grid showing Director's capabilities organized by the three-part loop:

- Section title (h2): "What Director Does"
- Section subtitle (p): "12 commands. Three stages of building. One coherent workflow."
- Feature cards in a responsive grid (3 columns desktop, 1 column mobile):

Card 1 -- Blueprint:
- Icon/emoji for planning
- Title: "Blueprint"
- Subtitle: "Plan Your Project"
- Description: "Director interviews you about what you want to build, captures a plain-language vision, and breaks it into goals, steps, and tasks. Update your plan anytime — Director adjusts without losing progress."
- Commands: `/director:onboard`, `/director:blueprint`

Card 2 -- Build:
- Icon/emoji for building
- Title: "Build"
- Subtitle: "Make Progress"
- Description: "Each task runs in a fresh AI workspace with only the context it needs. No accumulated confusion, no quality degradation. Progress is saved automatically after every task."
- Commands: `/director:build`, `/director:quick`

Card 3 -- Inspect:
- Icon/emoji for verification
- Title: "Inspect"
- Subtitle: "Check Your Work"
- Description: "Director verifies outcomes, not just task completion. Structural checks run automatically. Behavioral checklists help you confirm things actually work the way you intended."
- Commands: `/director:inspect`

Below the main 3, add a row of supporting features (smaller cards or a simpler list):
- "Pick up where you left off" — resume, status
- "Think freely" — brainstorm, idea
- "Change direction" — pivot, undo
- "Stay informed" — help, status, cost tracking

Use a 2x2 or 4-column grid for these smaller items.

**Why Director section (`#why`):**

- Section title (h2): "Why Director"
- Content addressing the gap per CONTEXT.md: "Other tools give you AI. Director gives you structure."
- Present 3-4 differentiation points, each as a short block with a bold statement and one supporting sentence:
  1. "Fresh context, every task" — "Each task gets a clean AI workspace loaded with just what it needs. No context rot, no quality degradation over long projects."
  2. "Your vision stays central" — "Everything Director builds traces back to what you described. Verification checks outcomes against your goals, not just task completion."
  3. "Plain language, always" — "No jargon, no config files, no YAML. Director speaks your language — what to build, not how to code."
  4. "Structure without overhead" — "12 commands cover the full lifecycle. No sprint ceremonies, no ticket management, no process theater. Just build."

Keep the tone confident and opinionated (per Stripe/Raycast feel). Do NOT name competitors.

**Commands section (`#commands`):**

- Section title (h2): "Commands"
- Section subtitle (p): "Everything you need to go from idea to working product."
- Display ALL 12 commands from the README in a clean layout. Use a table or card-list format:

  Blueprint:
  - `/director:onboard` — Set up a new project or map an existing one
  - `/director:blueprint` — Create, view, or update your gameplan

  Build:
  - `/director:build` — Work on the next ready task
  - `/director:quick "..."` — Make a fast change without full planning

  Inspect:
  - `/director:inspect` — Verify what was built matches the goal

  Other:
  - `/director:status` — See where you are and what's next
  - `/director:resume` — Pick up where you left off after a break
  - `/director:brainstorm` — Think out loud with full project context
  - `/director:pivot` — Handle a change in direction
  - `/director:idea "..."` — Capture an idea for later
  - `/director:undo` — Go back to before the last task
  - `/director:help` — Show all commands and your current status

- Use monospace font for command names, regular font for descriptions
- Group by Blueprint/Build/Inspect/Other with subtle group labels

**CSS styles to add:**

Feature cards:
- `.features-grid`: display grid, grid-template-columns repeat(3, 1fr), gap 1.5rem
- Below 768px: `.features-grid` grid-template-columns 1fr
- `.feature-card`: background var(--color-bg-elevated), border 1px solid var(--color-border), border-radius 0.75rem, padding 2rem, transition border-color 0.2s
- `.feature-card:hover`: border-color var(--color-border-accent)
- `.feature-card__icon`: font-size 1.5rem, margin-bottom 1rem
- `.feature-card__title`: font-size 1.25rem, font-weight 700, color var(--color-text-primary), margin-bottom 0.25rem
- `.feature-card__subtitle`: font-size 0.875rem, color var(--color-accent), font-weight 600, margin-bottom 0.75rem
- `.feature-card__desc`: color var(--color-text-secondary), line-height 1.6, font-size 0.9375rem, margin-bottom 1rem
- `.feature-card__commands`: font-family var(--font-mono), font-size 0.8125rem, color var(--color-text-muted)

Supporting features:
- `.supporting-grid`: display grid, grid-template-columns repeat(4, 1fr), gap 1rem, margin-top 2rem
- Below 768px: `.supporting-grid` grid-template-columns repeat(2, 1fr)
- `.supporting-item`: padding 1.25rem, border-left 2px solid var(--color-accent-subtle)
- `.supporting-item__title`: font-weight 600, color var(--color-text-primary), margin-bottom 0.25rem, font-size 0.9375rem
- `.supporting-item__desc`: font-size 0.8125rem, color var(--color-text-muted)

Why section:
- `.why-grid`: display grid, grid-template-columns repeat(2, 1fr), gap 2rem, margin-top 2rem
- Below 768px: `.why-grid` grid-template-columns 1fr
- `.why-item`: padding 1.5rem 0
- `.why-item__title`: font-size 1.125rem, font-weight 700, color var(--color-text-primary), margin-bottom 0.5rem
- `.why-item__desc`: color var(--color-text-secondary), line-height 1.6

Commands section:
- `.commands-group`: margin-bottom 2rem
- `.commands-group__label`: font-size 0.75rem, font-weight 600, text-transform uppercase, letter-spacing 0.1em, color var(--color-accent), margin-bottom 0.75rem
- `.commands-list`: list-style none, padding 0
- `.commands-list li`: display flex, gap 1rem, padding 0.5rem 0, border-bottom 1px solid var(--color-bg-overlay)
- `.commands-list li:last-child`: border-bottom none
- `.command-name`: font-family var(--font-mono), font-size 0.875rem, color var(--color-text-primary), white-space nowrap, min-width 14rem
- `.command-desc`: font-size 0.9375rem, color var(--color-text-secondary)
- Below 768px: `.commands-list li` flex-direction column, gap 0.25rem; `.command-name` min-width auto
  </action>
  <verify>
Open page in browser:
1. Features section shows 3 main cards (Blueprint, Build, Inspect) + 4 supporting features
2. Cards have hover effect (border color change)
3. Why section has 4 differentiation points, no competitor names
4. Commands section lists all 12 commands grouped by category
5. All sections are responsive (check at 375px and 1200px+)
6. Section anchors work from nav (clicking "Features" scrolls to features)
  </verify>
  <done>Features section shows the full Director capability set. Why section differentiates without naming competitors. Commands section lists all 12 commands with descriptions. All sections responsive and linked from navigation.</done>
</task>

</tasks>

<verification>
- Problem section tells an empathetic story about vibe coder struggles
- Terminal animation plays on scroll, demonstrates onboard -> blueprint -> build
- Replay button works
- Features section covers Blueprint/Build/Inspect with supporting capabilities
- Why section has 4 differentiation points, no competitor names mentioned
- Commands section lists all 12 commands with accurate descriptions (matching README)
- All new sections have correct IDs matching nav anchor links
- Responsive at mobile (375px) and desktop (1200px+)
</verification>

<success_criteria>
- Terminal animation triggers on viewport entry and replays on button click
- All content sections populated with compelling, scannable copy
- All 12 commands listed accurately
- No competitors named, but differentiation is clear
- Responsive layout for all new sections
</success_criteria>

<output>
After completion, create `.planning/phases/11-landing-page/11-02-SUMMARY.md`
</output>
