---
phase: 11-landing-page
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - site/index.html
  - site/css/styles.css
  - site/js/main.js
  - site/assets/og-image.png
  - .github/workflows/deploy-site.yml
autonomous: false

must_haves:
  truths:
    - "Mid-page Get Started section has the install command with copy button and GitHub link"
    - "Newsletter section has an email input form that posts to Buttondown"
    - "Footer shows Director logo, GitHub link, license, and credit"
    - "All content sections below the hero fade in as they scroll into view"
    - "Active navigation link highlights based on current scroll position"
    - "GitHub Actions workflow exists to deploy site/ to GitHub Pages"
    - "OG image exists for social sharing"
  artifacts:
    - path: "site/index.html"
      provides: "Complete landing page with all 9 sections"
      contains: "newsletter"
    - path: "site/js/main.js"
      provides: "Scroll reveal, scroll spy, terminal animation"
      contains: "IntersectionObserver"
    - path: ".github/workflows/deploy-site.yml"
      provides: "GitHub Actions workflow to deploy site/ to Pages"
      contains: "actions/deploy-pages"
    - path: "site/assets/og-image.png"
      provides: "1200x630 social sharing image"
  key_links:
    - from: "site/js/main.js"
      to: "site/index.html sections"
      via: "IntersectionObserver for scroll reveal and nav highlighting"
      pattern: "querySelectorAll.*\\.section"
    - from: ".github/workflows/deploy-site.yml"
      to: "site/"
      via: "GitHub Pages deployment source"
      pattern: "path.*site"
---

<objective>
Complete the landing page with bottom sections (Get Started CTA, Newsletter, Footer), add scroll animations and navigation intelligence, create deployment infrastructure, and verify the full page.

Purpose: Delivers the final conversion points (mid-page CTA, newsletter), adds polish (scroll reveals, active nav), and makes the site deployable to director.cc.
Output: A fully complete, deployment-ready landing page with all interactive behaviors.
</objective>

<execution_context>
@/Users/noahrasheta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/noahrasheta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-landing-page/11-CONTEXT.md
@.planning/phases/11-landing-page/11-RESEARCH.md
@.planning/phases/11-landing-page/11-01-SUMMARY.md
@.planning/phases/11-landing-page/11-02-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Get Started CTA, Newsletter form, Footer, scroll reveal, and scroll spy</name>
  <files>site/index.html, site/css/styles.css, site/js/main.js</files>
  <action>
**Get Started section (`#get-started`):**

This is the mid-page conversion point for visitors who watched the demo and read the features.

- Section title (h2): "Get Started"
- Subtitle: "Install Director in Claude Code and build your first project in minutes."
- Duplicate the hero CTA structure:
  - Install block with copy button (reuse the same copyInstall function -- use a data attribute or second ID like `installCmd2` and generalize the copy function to accept an element ID)
  - GitHub link button
  - Prerequisites note
- This section should feel like a natural pause point -- centered, spacious, clear CTAs.

**Newsletter section (`#newsletter`):**

Per CONTEXT.md: optional email signup for the developer's personal newsletter, non-intrusive, no popups, no gates.

- Section title (h2): "Stay in the loop"
- Description (p): "Get occasional updates on Director and other tools for vibe coders. No spam, unsubscribe anytime."
- Buttondown form (plain HTML, no JS required):
  ```html
  <form
    action="https://buttondown.com/api/emails/embed-subscribe/PLACEHOLDER"
    method="post"
    class="newsletter-form"
  >
    <input type="hidden" name="embed" value="1" />
    <input
      type="email"
      name="email"
      placeholder="your@email.com"
      required
      class="newsletter-input"
    />
    <button type="submit" class="btn btn--primary newsletter-btn">Subscribe</button>
  </form>
  ```
- Use "PLACEHOLDER" as the Buttondown username (the developer will replace it when they create their account). Add an HTML comment: `<!-- Replace PLACEHOLDER with your Buttondown username -->`
- Keep this section visually subtle -- smaller text, less padding than main content sections

**Footer (`#footer`):**

- Director logo (small, the wordmark) linking to `#hero`
- Links row: GitHub repo link, "MIT License" text
- Credit line: "Built by Noah Rasheta" (per research recommendation -- minimal footer)
- Copyright: the current year

**Scroll Reveal (add to `site/js/main.js`):**

Add IntersectionObserver-based scroll reveal for all `.section` elements below the hero:

```javascript
// Scroll reveal for sections
const revealObserver = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add('visible');
      revealObserver.unobserve(entry.target);
    }
  });
}, { threshold: 0.1 });

document.querySelectorAll('.section').forEach(el => {
  // Don't animate hero
  if (el.id !== 'hero') {
    revealObserver.observe(el);
  }
});
```

Add corresponding CSS:
```css
.section {
  opacity: 0;
  transform: translateY(2rem);
  transition: opacity 0.6s ease, transform 0.6s ease;
}
.section.visible, #hero {
  opacity: 1;
  transform: translateY(0);
}
@media (prefers-reduced-motion: reduce) {
  .section {
    opacity: 1;
    transform: none;
    transition: none;
  }
}
```

**Scroll Spy for Navigation (add to `site/js/main.js`):**

Highlight the active nav link based on which section is currently in the viewport:

```javascript
const navLinks = document.querySelectorAll('.nav-links a');
const sections = document.querySelectorAll('section[id]');

const spyObserver = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      navLinks.forEach(link => {
        link.classList.toggle('active', link.getAttribute('href') === `#${entry.target.id}`);
      });
    }
  });
}, { threshold: 0.3, rootMargin: '-80px 0px -50% 0px' });

sections.forEach(section => spyObserver.observe(section));
```

**Generalize the copy function** to work for both hero and mid-page install blocks:

Replace the inline copyInstall function with a more generic version that accepts a button element, and use `onclick="copyCmd(this)"` on both copy buttons. The function finds the sibling `<code>` and copies its text. OR use a single function with different element IDs.

**CSS additions:**

Newsletter:
- `.newsletter-form`: display flex, gap 0.75rem, max-width 28rem, margin 1.5rem auto 0 auto
- Below 768px: `.newsletter-form` flex-direction column
- `.newsletter-input`: flex 1, padding 0.75rem 1rem, background var(--color-bg-elevated), border 1px solid var(--color-border), border-radius 0.5rem, color var(--color-text-primary), font-family var(--font-body), font-size 1rem
- `.newsletter-input::placeholder`: color var(--color-text-muted)
- `.newsletter-input:focus`: outline none, border-color var(--color-accent)
- `.newsletter-btn`: white-space nowrap

Get Started section:
- `#get-started`: text-align center
- `#get-started .install-block`: margin 0 auto (center the install block)

Footer:
- `.footer`: padding 3rem 0, border-top 1px solid var(--color-border)
- `.footer .container`: display flex, flex-direction column, align-items center, gap 1rem, text-align center
- `.footer-logo img`: height 1.25rem, opacity 0.6
- `.footer-links`: display flex, gap 1.5rem, font-size 0.8125rem
- `.footer-links a`: color var(--color-text-muted), text-decoration none
- `.footer-links a:hover`: color var(--color-text-secondary)
- `.footer-credit`: font-size 0.75rem, color var(--color-text-muted)
  </action>
  <verify>
Open page in browser (`python3 -m http.server 8000 --directory site/`):
1. Scrolling through the page, sections fade in as they enter the viewport
2. Active nav link updates as you scroll through sections
3. Get Started section has working install command copy and GitHub link
4. Newsletter form has email input and subscribe button
5. Footer shows logo, GitHub link, MIT license, credit
6. Copy button works on BOTH the hero and mid-page install blocks
7. `prefers-reduced-motion` disables scroll animations (test in browser dev tools)
  </verify>
  <done>All bottom sections complete. Scroll reveal animations work. Nav scroll spy highlights active section. Newsletter form ready (needs Buttondown username). Footer is minimal and on-brand.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions deployment workflow and OG image</name>
  <files>.github/workflows/deploy-site.yml, site/assets/og-image.png</files>
  <action>
**GitHub Actions workflow (`.github/workflows/deploy-site.yml`):**

Create a deployment workflow that publishes the `site/` directory to GitHub Pages. Since GitHub Pages natively only supports root or `/docs`, we need a GitHub Actions workflow to deploy from `site/`.

```yaml
name: Deploy Landing Page

on:
  push:
    branches: [main]
    paths: ['site/**']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

Key points:
- Triggers only when files in `site/` change (or manual dispatch)
- Uses the official GitHub Pages actions (v4/v5)
- The `site/` directory is uploaded as the artifact and deployed
- No build step needed (plain HTML/CSS/JS)
- Concurrency prevents overlapping deployments

**OG Image (`site/assets/og-image.png`):**

Create a 1200x630px OG image for social sharing. Since we cannot generate images programmatically in this context, create an HTML file that can be screenshotted, OR create a simple SVG-based approach:

Option A (preferred -- create a simple OG image using an HTML canvas approach):
Create a helper script `site/assets/generate-og.html` that when opened in a browser, renders a 1200x630 canvas with:
- Dark background (#0A0A0A)
- The "/director" text in white, large, centered (since we can't easily embed the PNG in canvas, use the text directly)
- Tagline below: "Orchestration for Vibe Coders"
- Electric blue accent line
- The user can screenshot this or use it as a reference

Option B (simpler -- create a placeholder):
If generating an actual PNG is not feasible, create a simple SVG file at `site/assets/og-image.svg` with the dark background, "/director" text, and tagline, and note that it should be exported to PNG. Also update the meta tags to reference `.svg` if going this route.

**IMPORTANT:** Whichever approach, ensure the OG meta tags in `index.html` reference the correct file path. If creating the HTML generator, also create a note comment in index.html: `<!-- OG image: open site/assets/generate-og.html in browser and screenshot at 1200x630 -->`

Practically, the best approach: create `site/assets/og-image.svg` as the OG image source with exact dimensions 1200x630, containing:
- Solid dark background (#0A0A0A)
- "/director" in white Inter-like font (use sans-serif), large size (~80px), centered horizontally, positioned at 40% vertical
- "Orchestration for Vibe Coders" below in gray (#A1A1A1), smaller (~28px)
- A subtle accent line (#3B82F6) underneath

Then update the OG meta tags to use `og-image.svg` or note that SVG may not be supported by all social platforms (Twitter, LinkedIn prefer PNG). If SVG won't work reliably for OG, use a simple approach: create a minimal HTML-to-canvas script that draws the image and exports as PNG (via `canvas.toDataURL()`), or simply note it as a manual task.

**Decision:** Create the SVG version AND add a small utility page (`site/assets/generate-og.html`) that renders and offers a PNG download button using canvas. The OG meta tags should point to `og-image.png`. Add a comment to the generate page explaining how to use it.
  </action>
  <verify>
1. `.github/workflows/deploy-site.yml` exists and has valid YAML syntax: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-site.yml'))"`
2. Workflow triggers on push to main when site/ changes
3. Workflow uses actions/deploy-pages@v4
4. OG image asset exists at `site/assets/og-image.svg` (or .png)
5. OG meta tags in index.html point to the correct image path
  </verify>
  <done>GitHub Actions workflow deploys site/ to GitHub Pages on push. OG image exists for social sharing. Deployment infrastructure is complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete director.cc landing page with:
- Dark-mode design with Director branding (logo, electric blue accent)
- Bold hero with tagline, dual CTAs (copy-to-clipboard install + GitHub link)
- Sticky navigation with scroll spy (active section highlighting)
- Problem section describing vibe coder struggles
- Animated terminal walkthrough (onboard -> blueprint -> build)
- Features section (Blueprint/Build/Inspect + supporting features)
- Why Director differentiation section
- All 12 commands listed
- Mid-page Get Started CTA
- Newsletter signup form (Buttondown)
- Footer with credits
- Scroll reveal animations
- GitHub Actions deployment workflow
- OG image for social sharing
  </what-built>
  <how-to-verify>
1. Start a local server: `python3 -m http.server 8000 --directory site/`
2. Open http://localhost:8000 in your browser
3. Verify the hero: bold tagline visible, install command with Copy button, GitHub link
4. Click Copy on the install command — verify clipboard has the correct command
5. Scroll down — sections should fade in as they appear
6. Watch the terminal animation play (should type out onboard/blueprint/build commands)
7. Click the Replay button on the terminal
8. Check navigation: clicking nav links scrolls to correct sections
9. Verify scroll spy: active nav link highlights as you scroll
10. Check mid-page Get Started section has working copy button
11. Check newsletter form has email input and subscribe button
12. Check footer has logo, GitHub link, MIT license, credit
13. Resize to mobile (375px) — verify responsive layout
14. Check browser tab shows /d favicon
15. Review overall feel: does it feel like Stripe/Raycast? Bold, confident, dark-mode?
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the page design, content, or functionality</resume-signal>
</task>

</tasks>

<verification>
- All 9 sections have real content (no placeholders)
- Terminal animation triggers on scroll and replays on button click
- Copy-to-clipboard works on both hero and mid-page install blocks
- Scroll reveal animations play for all sections below hero
- Scroll spy correctly highlights active nav link
- Newsletter form structure is correct (Buttondown, with placeholder username)
- Footer is minimal and correct
- GitHub Actions workflow has valid YAML and correct triggers
- OG image exists and is referenced in meta tags
- Page is responsive at 375px and 1200px+
- prefers-reduced-motion disables all animations
</verification>

<success_criteria>
- Complete landing page with all 9 sections populated
- All interactive behaviors working (copy, terminal, scroll reveal, scroll spy)
- Deployment workflow ready (push to main deploys site/)
- OG image ready for social sharing
- Human verification confirms the page meets the bold, confident Stripe/Raycast aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/11-landing-page/11-03-SUMMARY.md`
</output>
